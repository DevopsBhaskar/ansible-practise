- name: loops
  hosts: local
  connection: local
  tasks: 
  - name: printing loop with item
    ansible.builtin.package:
      msg: " iteration items are {{ item }}"
    loop: 
    - linux
    - shell
    - ansible
    - terraform