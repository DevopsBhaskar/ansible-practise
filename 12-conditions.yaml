- name: Condition checking
  hosts: local
  connection: local
  vars:
    num: 10
  tasks: 
  - name: checking number is less than 10 or not
    ansible.builtin.debug:
      msg: "{{ num }} is less than 8"
    when num < 10